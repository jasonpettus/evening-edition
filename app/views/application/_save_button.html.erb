<% if user_logged_in? %>

  <%= form_tag(favorite_stories_path( story.id ), method: :get, value: "true", class: 'inline') do %>
    <%= submit_tag ' | Save', class: 'save_button' %>
  <% end %>

<% end %>
